import { StyleSheet } from "react-native";
const styles=StyleSheet.create({
   imageposition:{justifyContent:'center',alignItems:'center'},
   image:{resizeMode: 'contain', width: 200, height:200,marginBottom:'10%' }
   
})
export default styles